extends ../layouts/application.pug

block header
  ='Add new label'

block content
  +formFor()(action='/label' method='post')
    +input('name', data.label.name, errors.name)(type="name")
    +submit()(value='Add label')
