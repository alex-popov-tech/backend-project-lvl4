extends ../layouts/application.pug

block header
  =t('views.new.statuses.title')

block content
  +formFor()(action=route('createStatus') method='post')
    +input(data.status, 'name', errors)
    +submit()(value=t('views.buttons.create'))
