extends ../layouts/application.pug

block header
  ='Add new Status'

block content
  +formFor()(action='/status' method='post')
    +input('name', data.name, errors.name)(type="name")
    +submit()(value='Add Status')
