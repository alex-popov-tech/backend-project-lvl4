extends ../layouts/application.pug

block header
  =t('views.new.session.title')

block content
  +formFor()(action='/sessions' method='post')
    +input(data.user, 'email', errors)(type="text")
    +input(data.user, 'password', errors)(type="password")
    +submit()(value=t('views.buttons.signin'))
