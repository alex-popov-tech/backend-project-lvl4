extends ../layouts/application.pug

block header
  =t('views.edit.labels.title')

block content
  +formFor()(action=`${route('updateLabel', { id: reply.request.params.id })}?_method=patch` method='post')
    +input(data.label, 'name', errors)
    +submit()(value=t('views.buttons.edit'))
